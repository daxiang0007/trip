# 本仓库约定

这是一个旅行记录站点,通过 GitHub Pages 发布。**任何 agent 在本仓库工作前先读这份文件。**

## 基本信息

| | |
|---|---|
| 仓库 | `daxiang0007/trip` |
| 分支 | `main`(唯一分支,直接提交,不开 PR) |
| 发布 | GitHub Pages,`main` 分支根目录 |
| 站点根 | https://daxiang0007.github.io/trip/ |

推送到 `main` 后约 1 分钟自动重新发布,无需任何额外操作。

## 目录结构

**一个目的地一个子目录,目录名用小写拼音或英文。**

```
/
├── index.html          总目录页,列出全部游记
├── CONVENTION.md       本文件
├── zhaoqing/
│   ├── index.html      肇庆正文
│   └── img/            该篇图片(可选)
└── guilin/
    ├── index.html
    └── img/
```

对应链接:

```
/trip/            总目录
/trip/zhaoqing/   肇庆
/trip/guilin/     桂林
```

## 新增一篇游记的步骤

1. **建正文** —— 新建 `<地名拼音>/index.html`。
   不需要先创建目录,直接写文件到该路径即可(Git 无空目录概念)。
2. **挂到目录页** —— 编辑根目录 `index.html`,在 `<!-- TRIPS -->` 标记下面按现有格式加一条。
3. 完成。推送后自动发布。

## 命名规则

- 目录名:**小写拼音,不带声调,多字连写或用连字符**。如 `zhaoqing`、`dali`、`inner-mongolia`
- **不要用中文做目录名**——GitHub 会剥掉中文字符,路径会坏掉
- 图片放各篇自己的 `img/` 下,不要堆在根目录

## 写作与样式

- 每篇正文**自带样式**,单文件自包含,不引用外部 CSS
- 保持与现有页面一致:暖米色纸底、深墨色文字、支持深色模式(用 `prefers-color-scheme`)
- 正文页顶部保留 `← 全部游记` 返回链接,指向 `../`
- 中文排版注意:HTML 源码里**中文句子中间不要换行**,否则渲染出来会多出空格

## 注意

- 本站是**公开**的,任何人拿到链接都能访问。**不要放客户材料、财务数据、私人信息。**
- GitHub Pages 无访问控制,私密内容请另寻他处。
